# 📋 Инструкция по настройке сайта Jastar Apartments

## Шаг 1: Создание Firebase проекта

1. Перейдите на https://console.firebase.google.com/
2. Нажмите **"Добавить проект"** или **"Create a project"**
3. Введите название проекта (например: `jastar-apartments`)
4. Следуйте инструкциям мастера создания проекта
5. **НЕ включайте** Google Analytics (не обязательно)

## Шаг 2: Настройка Authentication

1. В Firebase Console перейдите в **Authentication** (в левом меню)
2. Нажмите **"Get started"** или **"Начать"**
3. Перейдите на вкладку **Sign-in method**
4. Нажмите на **Email/Password**
5. Включите переключатель и нажмите **"Save"**

## Шаг 3: Создание админ аккаунта

1. В Firebase Console → **Authentication** → **Users**
2. Нажмите **"Add user"** или **"Добавить пользователя"**
3. Введите:
   - **Email**: `admin@jastar.kz` (или любой другой email)
   - **Password**: создайте надежный пароль
4. Нажмите **"Add user"**
5. **Сохраните email и пароль** - они понадобятся для входа в админ панель!

## Шаг 4: Настройка Firestore Database

1. В Firebase Console перейдите в **Firestore Database**
2. Нажмите **"Create database"** или **"Создать базу данных"**
3. Выберите **"Start in test mode"** (мы настроим правила позже)
4. Выберите регион (ближайший к Казахстану, например: `europe-west`)
5. Нажмите **"Enable"** или **"Включить"**

## Шаг 5: Настройка правил безопасности Firestore

1. В Firebase Console → **Firestore Database** → **Rules**
2. Замените содержимое на правила из файла `firestore.rules`:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{roomId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    match /applications/{applicationId} {
      allow create: if true;
      allow read, write: if request.auth != null;
    }
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

3. Нажмите **"Publish"** или **"Опубликовать"**

## Шаг 6: Получение конфигурации Firebase

1. В Firebase Console нажмите на **⚙️ Settings** (настройки проекта)
2. Прокрутите вниз до секции **"Your apps"** или **"Ваши приложения"**
3. Если приложения нет, нажмите **"Add app"** → выберите **Web** (</>)
4. Дайте имя приложению (например: `Jastar Website`)
5. Нажмите **"Register app"**
6. Скопируйте конфигурацию (она выглядит так):

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "jastar-apartments.firebaseapp.com",
  projectId: "jastar-apartments",
  storageBucket: "jastar-apartments.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

## Шаг 7: Вставка конфигурации в проект

1. Откройте файл `js/firebase-config.js`
2. Замените все значения `YOUR_*` на реальные значения из шага 6
3. Сохраните файл

## Шаг 8: Инициализация начальных данных

1. Откройте `index.html` в браузере
2. Откройте консоль разработчика (F12 или Ctrl+Shift+I)
3. Перейдите на вкладку **Console**
4. Введите команду:

```javascript
initializeRooms()
```

5. Нажмите Enter
6. Если увидите сообщение "Комнаты успешно инициализированы" - все готово!

## Шаг 9: Проверка работы

1. Откройте `index.html` в браузере
2. Вы должны увидеть главную страницу с комнатами
3. Перейдите на `admin.html`
4. Войдите используя email и пароль из шага 3
5. Вы должны увидеть админ панель со статистикой

## ✅ Готово!

Теперь сайт полностью настроен и готов к использованию!

### Что дальше?

- **Изменить комнаты**: Войдите в админ панель → вкладка "Комнаты" → редактируйте или добавляйте новые
- **Просматривать заявки**: В админ панели → вкладка "Заявки"
- **Изменить контакты**: Отредактируйте файл `index.html` (секция контактов)

## 🚀 Размещение на хостинге

### GitHub Pages (бесплатно)

1. Создайте репозиторий на GitHub
2. Загрузите все файлы проекта
3. В настройках репозитория → Pages → выберите ветку `main`
4. Сайт будет доступен по адресу: `https://ваш-username.github.io/репозиторий/`

### Netlify (бесплатно)

1. Перейдите на https://netlify.com
2. Зарегистрируйтесь (можно через GitHub)
3. Нажмите **"Add new site"** → **"Deploy manually"**
4. Перетащите папку проекта
5. Сайт будет доступен сразу!

## ⚠️ Важные замечания

- **Не публикуйте** файл `firebase-config.js` с реальными данными в публичных репозиториях
- Используйте **переменные окружения** или **Firebase Hosting** для продакшена
- Регулярно **проверяйте заявки** в админ панели
- **Делайте резервные копии** данных из Firestore

## 🆘 Проблемы?

Если что-то не работает:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что Firebase конфигурация правильная
3. Проверьте, что правила Firestore опубликованы
4. Убедитесь, что Authentication включен

## 📞 Поддержка

Если нужна помощь - проверьте:
- [Firebase Documentation](https://firebase.google.com/docs)
- [Firestore Rules Guide](https://firebase.google.com/docs/firestore/security/get-started)


