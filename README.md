# Jastar Apartments - Сайт общежития

Современный сайт общежития с админ панелью для управления заявками и комнатами.

## 🚀 Быстрый старт

### 1. Настройка Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Создайте новый проект (или используйте существующий)
3. Включите следующие сервисы:
   - **Authentication** (Email/Password)
   - **Firestore Database**

### 2. Получение конфигурации

1. В Firebase Console перейдите в **Project Settings** (⚙️)
2. Прокрутите до секции **Your apps** и добавьте **Web app** если еще не добавлен
3. Скопируйте конфигурацию Firebase
4. Откройте `js/firebase-config.js` и замените значения:

```javascript
const firebaseConfig = {
  apiKey: "ВАШ_API_KEY",
  authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
  projectId: "ВАШ_PROJECT_ID",
  storageBucket: "ВАШ_PROJECT_ID.appspot.com",
  messagingSenderId: "ВАШ_MESSAGING_SENDER_ID",
  appId: "ВАШ_APP_ID"
};
```

### 3. Настройка Firestore правил безопасности

В Firebase Console перейдите в **Firestore Database** → **Rules** и установите следующие правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Публичный доступ: чтение комнат, создание заявок
    match /rooms/{roomId} {
      allow read: if true;
      allow write: if request.auth != null; // Только авторизованные админы
    }
    
    match /applications/{applicationId} {
      allow create: if true; // Публичное создание заявок
      allow read, write: if request.auth != null; // Только админы могут читать/изменять
    }
    
    // Все остальные коллекции доступны только админам
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

### 4. Создание админ аккаунта

1. В Firebase Console перейдите в **Authentication**
2. Включите провайдер **Email/Password**
3. Перейдите на вкладку **Users**
4. Нажмите **Add user** и создайте админ аккаунт:
   - Email: `admin@jastar.kz` (или любой другой)
   - Password: (создайте надежный пароль)

### 5. Инициализация данных

1. Откройте `index.html` в браузере
2. Откройте консоль разработчика (F12)
3. Выполните команду для инициализации начальных комнат:

```javascript
// Эта функция автоматически добавит начальные данные при первом запуске
// Или выполните вручную:
const { initializeRooms } = await import('./js/rooms-data.js');
initializeRooms();
```

### 6. Запуск сайта

Просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
# Python
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server
```

Затем откройте `http://localhost:8000`

## 📁 Структура проекта

```
project/
├── index.html              # Главная страница (публичная)
├── admin.html              # Админ панель
├── js/
│   ├── firebase-config.js  # Конфигурация Firebase
│   ├── auth.js             # Аутентификация
│   ├── rooms-data.js       # Функции для работы с комнатами
│   ├── script.js           # Основная логика сайта
│   └── admin.js            # Логика админ панели
├── css/
│   └── style.css           # Дополнительные стили
└── README.md               # Этот файл
```

## ✨ Функции

### Публичная часть (index.html)
- ✅ Главная страница с баннером
- ✅ Каталог комнат из Firestore
- ✅ Модальные окна с деталями комнат
- ✅ Форма заявки на проживание
- ✅ Секция "О нас"
- ✅ Контакты (адрес, телефон, Instagram, 2ГИС)

### Админ панель (admin.html)
- ✅ Авторизация по email/паролю
- ✅ Дашборд со статистикой заявок
- ✅ Управление заявками (просмотр, одобрение, отклонение)
- ✅ Фильтры заявок (все, новые, одобренные, отклоненные)
- ✅ Управление комнатами (добавление, редактирование, удаление)
- ✅ Редактирование цен, описаний, фото

## 🔒 Безопасность

- Все данные хранятся в Firebase
- Админ панель защищена авторизацией
- Правила Firestore ограничивают доступ к данным
- Публичный доступ: только чтение комнат и создание заявок

## 📱 Адаптивность

Сайт полностью адаптирован для:
- 💻 Десктопов
- 📱 Планшетов
- 📱 Мобильных устройств

## 🛠 Технологии

- **HTML5** + **CSS3**
- **Tailwind CSS** (CDN)
- **JavaScript** (ES6+)
- **Firebase** (Firestore + Authentication)
- **Font Awesome** (иконки)

## 📞 Контакты

- **Адрес**: ул. Каныша Сатпаева, 7А, район Бостандык, Алматы
- **Телефон**: +7 707-498-68-36
- **Instagram**: [@jastar_apartments](https://instagram.com/jastar_apartments)
- **2ГИС**: [Открыть на карте](https://2gis.kz/almaty/search/ул.%20Каныша%20Сатпаева%207А)

## 📝 Лицензия

Все права защищены © 2024 Jastar Apartments


